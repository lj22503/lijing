# Agent 友好网站规范

> 适用于所有后续网站/内容项目。参考来源：燃冰提供的 Agent 友好标准 + Schema.org + llms.txt 规范。

---

## 8 条规则

### 1. 结构化内容

- 使用清晰标题层级：`<h1>` 页面标题 → `<h2>` 区块 → `<h3>` 子区块
- 关键信息用列表 `<ul>/<ol>` 或表格 `<table>`，不埋在大段文字里
- 每个页面有明确主题，不混杂多个话题

### 2. 语义化标记

- 每个页面在 `<head>` 中嵌入 JSON-LD
- 个人站点用 `Person`，项目/公司用 `Organization`，文章用 `Article`，问答用 `FAQPage`
- JSON-LD 格式示例见 `index.html` `<head>` 部分

### 3. 核心信息页

- About 页：姓名、身份、业务范围、目标用户
- Products/Services 页：每个产品独立描述，含名称/说明/链接
- 不依赖 JS 渲染核心内容（Agent 爬虫可能不执行 JS）

### 4. llms.txt

- 网站根目录放置 `llms.txt`，格式：
  - `# 站点名 — Agent 导航`
  - `## 关于` — 一句话介绍
  - `## 核心页面` — 关键页面链接
  - `## 品牌关键词` — SEO 关键词
  - `## 项目` — 主要项目及链接

### 5. 高质量可引用内容

- 用具体数据替代模糊表述（"使用量5.9万+" 而非 "很多用户"）
- 文章标注作者、发布日期、来源引用
- 案例/数据提升 EEAT（经验、专业性、权威性、可信度）

### 6. AI 可访问性

- ✅ 核心内容在首屏 HTML 中直接可见
- ✅ 图片加有意义的 `alt` 文字
- ✅ 不在 `robots.txt` 屏蔽主流 AI 爬虫（GPTBot, ClaudeBot 等）
- ❌ 避免 JS 渲染核心内容（如有，加 `<noscript>` 或 JSON-LD 兜底）

### 7. 内容协商（进阶）

- 支持 `Accept: text/markdown` 请求头 → 返回精简 Markdown
- 可选：`llms-full.txt` 提供完整站点文本（供 LLM 训练抓取）

### 8. 链接即地图

- 首页/导航有清晰内容目录
- 链接文字语义化（`/docs/kyc` 而非 `/page?id=42`）
- 层级不超过 3 层

---

## 清单（每次上线前检查）

- [ ] JSON-LD schema 已嵌入
- [ ] 核心内容 HTML 可见（不依赖 JS）
- [ ] llms.txt 在根目录
- [ ] 图片有 alt 文字
- [ ] robots.txt 未屏蔽 AI 爬虫
- [ ] 标题层级清晰（H1→H2→H3）
